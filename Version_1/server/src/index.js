import express from 'express' //serve as framework to create api. Serves frontend
import cors from 'cors' //libary, sets up rules for frontend and backend
import mongoose from 'mongoose' //write queries to database

